<% if book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(book.errors.count, "error") %> prohibited this product from being saved:</h2>
    <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="container">
  <div class="row">
    <div class='col-xs-3'>

      <h2>New book</h2>

      <%= form_for(book) do |f| %>



      	<h4>Title</h4>
      	<%= f.text_field :title, autofocus: true, class: "col-xs-12 form-control" %>

      	<h4>Opinion</h4>
      	<%= f.text_area :body, autofocus: true, class: "col-xs-12 form-control" %>

      	<%= f.submit 'Create Book',{class: "col-xs-12 btn btn-primary"} %>

      <% end %>

    </div>

  </div>
</div>